# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Virtualenv
.venv/
venv/
ENV/

# Django / локальные данные
*.log
qcluster.log
logs/
db.sqlite3
db.sqlite3.SERVER_BACKUP
db.sqlite3-*
db.sqlite3.corrupt*
# Восстановление БД / спасательные дампы (не в репозиторий)
rescued_*.json
*.broken
*.REBORN_FINAL
media/
# Собранная статика Django (только каталог в корне репозитория, нижний регистр).
# Не использовать шаблон «static/» без слэша — на Windows он совпадает с папкой STATIC/.
/static/

# Кэши и runtime
cache/
qcache/
tmp/
*.sqlite3-shm
*.sqlite3-wal

# Локальный rsync БД/media (хост/путь к серверу не в репозитории)
sync_data.sh

# Секреты и окружение (ключи держите в env или в приватном репо)
.env
.env.*
!.env.example

# Журнал работ только на этом ПК (не в репозитории)
docs/ЖУРНАЛ_ЛОКАЛЬНЫЙ.md

# Личный файл владельца (только локально; ассистент не открывает и не меняет)
INFO_START.txt

body.txt

# Node (сборка Tailwind: npm ci && npm run build:css)
node_modules/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
nul

# Прочее
.coverage
htmlcov/
.mypy_cache/
.pytest_cache/

# Архивы из docs не в репозиторий
docs/*.zip

# Локальные дампы БД (не в git)
final_reborn_dump.json
*_dump.json
