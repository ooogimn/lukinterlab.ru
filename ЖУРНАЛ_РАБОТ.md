# Журнал работ — lukinterlab.ru

Краткая хронология изменений и деплоя. Дополняйте при существенных шагах.

## 2026-03-31

- **Статика / админка:** `STATICFILES_DIRS` только если папка `STATIC` есть (убрать W004 на хостинге). Комментарий про конфликт `vendor/adminlte` с Jazzmin.
- **Спам-регистрация:** модуль `home/registration_guards.py`, проверки в `home/forms.py` (регистрация, заказ с аккаунтом, профиль), honeypot `company_fax` в шаблонах `register.html`, `checkout.html`.
- **Секреты:** вынесены в переменные окружения — `ALUKINTERLAB/secrets_env.py`, загрузка `.env` через `python-dotenv`, шаблон `env.example`; правки `settings.py`, `INFO_START.txt`, сообщение в `Blog/vk_utils.py`.
- **Git:** добавлен `.gitignore`; инициализация репо, первый push на `https://github.com/ooogimn/lukinterlab.ru.git` (ветка `main`). Удалён ошибочный вложенный клон `lukinterlab.ru/lukinterlab.ru` после `git clone` внутри проекта.
- **Правило Cursor:** `.cursor/rules/lukinterlab-commands-and-sync.mdc` — одна команда за раз на сервере; синхрон локалка / Git / хостинг; обновление этого журнала.
- **Git:** коммит `07af862` (правило + журнал) запушен в `origin/main`.

### Дальше по деплою (чеклист)

- [ ] На сервере: актуальный `git pull`, файл `.env` (в т.ч. `DJANGO_DEBUG=False`), `migrate`, `collectstatic`, перезапуск приложения.
- [ ] Убедиться, что публичный репозиторий не содержит секретов в истории; при необходимости ротация ключей после миграции на `.env`.
